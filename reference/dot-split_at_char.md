# .split_at_char

Split string by deliminating character

## Usage

``` r
.split_at_char(line, delim = ",")
```

## Arguments

- line, :

  String containing data to split

- delim, :

  Character to split data by

## Value

A vector of Strings
